---
- name: Install Tools
  become: true
  ansible.builtin.apt:
    name:
      - git
      - jq
    update_cache: true

- name: Transfer .gitconfig
  ansible.builtin.copy:
    src: ".gitconfig"
    dest: "{{ ansible_env.HOME }}/"
    mode: "0644"
